<%- include('./../trimmedheader.ejs') %>
<div class="container">
    <div class="row col st-page-top-panel">
        <div class="st-page-title">
            <h4><i class="fas fa-file-alt"></i>&nbsp;&nbsp;Edit Entry</h4>
        </div>
    </div>
    <div class="row col st-table st-main-content">
        <ul class="nav nav-tabs">
            <li class="nav-item">
                <a class="nav-link active" href="#formArticle" data-toggle="tab">Content</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#prvwArticle" data-toggle="tab">Preview</a>
            </li>
        </ul>
        <div class="tab-content">
            <div id="formArticle" class="tab-pane show active">
                <form action="/articles/update/<%= article.id %>?_method=PUT" method="POST" class="st-article-form" id="frmArticle">
                    <%- include('_diaryform.ejs') %>
                    <!-- <div class="form-group">
                        <label for="title">Title</label>
                        <input type="text" class="form-control" name="title" value="<%= (article.title != undefined) ? article.title : '' %>" required>
                    </div>
                    <div class="form-group">
                        <label for="category">Category</label>
                        <select name="category" id="articleCategory" class="form-control" required>
                            <% if (categories != undefined && categories.length > 0) { %>
                                <% categories.forEach(category => { %>
                                    <option value="<%= category.id %>"><%= category.name%></option>
                                <% }) %>
                            <% } %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="content">Content</label>
                        <textarea name="content" id="articleText" rows="20" class="form-control" type="text" required><%= (article.content != undefined) ? article.content : "" %></textarea>
                    </div> -->
                    <div>
                        <button type="submit" class="btn st-btn-primary">Save</button>
                        <a href="/articles" class="btn st-btn-primary" role="button">Cancel</a>
                        <% if (article.id != undefined) { %>
                        <input type="hidden" name="articleid" value="<%= article.id%>">
                        <% } %>
                    </div>
                </form>
            </div>
            <div class="tab-pane st-article-preview" id="prvwArticle">
                This will be article preview.
            </div>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script src="/js/articlepage.js"></script>
<%- include('./../footer.ejs') %>
